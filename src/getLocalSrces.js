import * as cheerio from 'cheerio';

const tagAttrMap = [
  {
    tag: 'img',
    attr: 'src',
  },
  {
    tag: 'link',
    attr: 'href',
  },
  {
    tag: 'script',
    attr: 'src',
  },
];

const getLocalSrces = async (html, url) => {


  return 
};

export default // getLocalSrces;
